import React from 'react';

const style = {
    paddingTop: "50px",
    paddingBottom: "50px"
}

const Title = () => (
    <div style={style}>
    <center>
        <h1>Commulearn</h1>
        <h5>Find online communities around topics of your interest.</h5>
    </center>
    </div>
);

export default Title;